server:
  port: 9999

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/EnocaDB
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: create
      format_sql: true
    show-sql: true